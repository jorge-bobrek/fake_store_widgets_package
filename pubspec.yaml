name: fake_store_widgets_package
description: "Sistema de Diseño para la API de Fake Store basado en Atomic Design"
version: 0.0.2
homepage: https://github.com/jorge-bobrek/atomic_design

environment:
  sdk: '>=3.3.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true